import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const CreateTweet = () => {
  return (
    <View>
      <Text>CreateTweet</Text>
    </View>
  );
};

export default CreateTweet;

const styles = StyleSheet.create({});
